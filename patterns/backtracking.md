# Backtracking

## Recognition
A sequence of choices must be explored under constraints, with invalid branches pruned.

## Core Idea
Choose → recurse → undo. Define state precisely and prune as early as correctness allows.
