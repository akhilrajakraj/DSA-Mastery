# Arrays

Store concepts, from-scratch implementations, and problems for arrays here.

- `concepts/` — notes and invariants
- `implementations/` — core algorithms
- `easy/`, `medium/`, `hard/` — problem practice
