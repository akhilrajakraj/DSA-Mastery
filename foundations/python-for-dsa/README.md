# Python for DSA

Focus on lists, tuples, sets, dictionaries, strings, slicing, functions, recursion, `collections`, `heapq`, iterators, references, and clean input/output.

Goal: Python should never be the bottleneck when reasoning about an algorithm.
