# 18 — Trie

Learn prefix trees, insertion/search, prefix queries, word dictionaries, and trie-based optimization.

Mastery: recognize prefix-structured data and choose a trie when repeated prefix traversal makes hashing or scanning inefficient.
