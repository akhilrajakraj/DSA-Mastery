# 15 — Greedy Algorithms

Learn local-choice strategies, exchange arguments, interval scheduling, sorting-based greedy methods, and when greedy fails.

Mastery: justify why a local choice can be extended to an optimal global solution rather than relying on intuition alone.
